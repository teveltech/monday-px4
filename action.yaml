name: 'Add px4 releases to monday'
description: 'Add px4 releases to monday'
branding:
  icon: 'anchor'
  color: 'orange'
inputs:
  url:
    description: ''
    required: true
  tag:
    description: ''
    required: true
  token:
    description: 'Monday authentication token to use'
    required: true
outputs:
  item-id: 
    description: ''
runs:
  using: 'node12'
  main: 'index.js'
